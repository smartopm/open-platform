/* eslint-disable */

import './commands'

// Cypress.on('uncaught:exception', (err, runnable) => {
//   // returning false here prevents Cypress from
// // failing the test
//   return false
// })

// beforeEach(() => {
//   // cy.resetDatabase()
// })

// after(() => {
//   // cy.resetDatabase()
// })
